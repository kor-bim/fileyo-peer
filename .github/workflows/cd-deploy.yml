name: CD Deploy (Self-Hosted)

on:
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: deploy-fileyo-peer
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: [self-hosted, macOS]
    steps:
      - name: Deploy peer on production machine
        run: /Users/hanbim/Development/fileyo-workspace/deploy/production/scripts/deploy-from-push.sh fileyo-peer
